name = "gvj-blog-intake"
main = "index.js"
compatibility_date = "2024-01-01"

# Environment variables are set in Cloudflare Dashboard (not here, to avoid committing secrets).
# Go to: Workers & Pages → gvj-blog-intake → Settings → Variables and Secrets
# Add:
#   INTAKE_SECRET      (secret)  — a random string you choose; give this to the co-work agent
#   FIREBASE_API_KEY   (text)    — AIzaSyCcoyBXRITTblrH0XcaLGMjftUttXYLtj4
#   FIREBASE_EMAIL     (text)    — office@globalvisajourneys.com
#   FIREBASE_PASSWORD  (secret)  — AuntyAisha@2023
#   FIREBASE_PROJECT   (text)    — global-visa-journeys
